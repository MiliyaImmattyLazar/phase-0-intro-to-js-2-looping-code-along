// Code your solutions in this file

/*const strings=[]
let newArray=[]
function writecards(strings,eventName){
    for(i=0;i<strings.length;i++)
    {
        console.log(`Thankyou ${strings[i]} for wonderful ${eventName} gift`)
        
         newArray.push(`Thankyou ${strings[i]} for wonderful ${eventName} gift`)
        
    }
    console.log(newArray)

}
writecards(["Charlie", "Samip", "Ali"], "birthday")*/

function countDown(num) {
  while (num >= 0) {
    console.log(num);
    num--;
  }
}

countDown(10);